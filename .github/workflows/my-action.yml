name: presentacion actions y proyects nogadev
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hola mundo!"
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Istalación de dependecias
        run: npm ci
      - name: correr eslint
        run: npx eslint
